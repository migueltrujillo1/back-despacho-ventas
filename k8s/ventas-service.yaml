apiVersion: v1
kind: Service
metadata:
  name: ventas-backend
  namespace: ep3
spec:
  type: ClusterIP
  selector:
    app: ventas-backend
  ports:
    - name: http
      port: 8082
      targetPort: 8082
